cat(date(),'\n', file = stderr())
options(scipen=100)

if (interactive()) {
  .First <- function() try(utils::loadhistory("~/.Rhistory"))
  .Last <- function() try(utils::savehistory("~/.Rhistory"))
}
